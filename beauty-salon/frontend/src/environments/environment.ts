/**
 * Configuración del entorno.
 *
 * ════════════════════════════════════════════════════
 *  EDITAR cuando el backend esté listo
 * ════════════════════════════════════════════════════
 *
 * production → false en desarrollo, true en producción
 * apiUrl     → base URL del backend (sin trailing slash)
 */

export const environment = {
  production: false,
  apiUrl: '/api',
};
